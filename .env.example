# Cambia TU_PASSWORD por la contraseña de tu MySQL (usuario root por defecto).
# Si tu contraseña tiene caracteres especiales (@ : / # ?) codifícalos, p. ej. @ -> %40
DATABASE_URL="mysql://root:TU_PASSWORD@localhost:3306/gimnasio"
